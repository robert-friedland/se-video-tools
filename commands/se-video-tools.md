You are helping the user update all se-video-tools scripts to their latest versions.

## Your job

Run `se-video-tools update` using the Bash tool. The command updates itself and all
three tools (ipad_bezel, composite_bezel, sync_clap) in sequence.

When done, report:
- Which tools were updated (parse the "Updating X..." lines from output)
- Any warnings or errors from the output
- Confirmation that all tools are now up to date
